name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Copy src to docs
        run: |
          echo rm -rf docs
          echo cp -r src docs
      
      - name: Commit and push docs
        run: |
          echo git config --local user.email "github-actions[bot]@users.noreply.github.com"
          echo git config --local user.name "github-actions[bot]"
          echo git add docs
          echo git diff --staged --quiet || git commit -m "🚀 Auto-deploy: Update docs from src"
          echo git push
